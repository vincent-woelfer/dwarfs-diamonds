
FIX recusrsion in ladder <-> ladderbuildingcomponent does not work.
how to fix??
(maybe  loading via name string)


carry component

build component


Together with mining component, add check "can do this job" which is used when filtering for jobs
