########################################################################################################################
# TODO
########################################################################################################################

============ Implement Important ============
Bis einschl. 1.2. ALPHA-0.1

Gebäude-System, heißt:
Schutt+Diamanten abladen. 
Diamanten zählen (simpler Highscore)
Diamanten spawnen in Gestein. 

Entweder lift oder minecard mit laden/entladen und ins pathfinding eingebunden. 



ActionPoints aus ActionPointsRes erstellen wenn building completed ist.





============ Implement Nice to Have ============
- VFX-Manager (or particles / effects manager)
-> global instance (from level i guess) to spwan particle effects like dust uppon destroyed cell


- Can build ladder from other ladder (so one can build up)

- Rubble change sprite. oon ground = dirt, carried = bag


============ Buildings Future ============
Building similar to dwarf, features are from different components:
- InteractionPoint (prev "entrance"):
One cell (grid_object_2d) but also precise point in cell.
Has Actions: Load, unload, refill-whatever

Connected to (for example):
Storage-Component: Shared by interactionPoints, similar (same?) like dwarfs carryingcomponent.


===== OLD: =====
One BuildingBase (fixed pos) contains:
- Entrances (fixed i guess). No own sprite
- moving platforms (elevators, mining cards ...). Contains own sprite

Platforms are either boarded at entrances or everywhere (mine card) when standing still.

Entrances can also only have a function like deposit, not necessarily moving platform.

Decide: entrance -> board platform or is platform for boarding logic?
-> Platform has board & load (for stuff other than dwarfs) logic.
Entrances (if for platform) are only responsible a waiting-for-platform-arrives point or as a deposit (for loading into and after loading out of minecards/platforms).
Platforms are only boardabile if still and at entrance, exception for maybe minecards.




============ Refactor ============
- MAYBE split BuildingData into Ressource and instantiated object



########################################################################################################################
# DOCUMENTATION
########################################################################################################################

JOB:
- Globale Aufgabe. Muss erledigt werden, kann von allen (mit Einschränkungen natürlich) bearbeitet werden, manchmal sogar mehrere auf einmal.
- Wird erstellt von dem was bearbeiter werden muss (building für construction, cell für mining, rubble für pickup usw.)


Dwarf-Task-Logic:
beinhaltet:
- jobs
- pfade (plural!)
- Action Points
- "needs" wie torch refill, rubble disposal
- Interactionen (Torch Placement?)

-> Eventuell stack/liste mit "teilschritten" des aktuellen plans. Das ist immer max ein job (der halt aus unterschritten besteht) oder kein job sondern "eigene ziele"/needs


-> Würde auch die ganze JobWithPath Logic vereinfachen denke ich.
-> Dwarf sollte eigene TaskQueue oder so als Component bekommen.
-> Sorgt auch dafür das leichter dinge dazwischen kommen können, z.B. torch placen ohne dass es vorher gepplant ist.

Dwarf State Machine:
-> Umfasst nur States die der Dwarf physisch ausführt (laufen, mining, bauen), kein "Planning".

