Add building to all relevant cells,
-> Correctly check if building can be placed based on occupued cells

carry component

build component


Together with mining component, add check "can do this job" which is used when filtering for jobs
